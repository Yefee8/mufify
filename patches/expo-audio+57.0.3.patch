diff --git a/node_modules/expo-audio/android/src/main/java/expo/modules/audio/AudioModule.kt b/node_modules/expo-audio/android/src/main/java/expo/modules/audio/AudioModule.kt
--- a/node_modules/expo-audio/android/src/main/java/expo/modules/audio/AudioModule.kt
+++ b/node_modules/expo-audio/android/src/main/java/expo/modules/audio/AudioModule.kt
@@ -145,14 +145,20 @@
       return
     }
 
+    /*
+     * `doNotMix` asks for focus outright, not transiently. Transient focus
+     * is for a clip: whoever was playing before pauses and resumes the moment
+     * it is released — so pausing this app brought the other player back, and
+     * on its own. A music player that starts means the last one has stopped.
+     */
     val result = if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {
       val requestType = interruptionMode?.let {
         if (it == InterruptionMode.DO_NOT_MIX) {
-          AudioManager.AUDIOFOCUS_GAIN_TRANSIENT
+          AudioManager.AUDIOFOCUS_GAIN
         } else {
           AudioManager.AUDIOFOCUS_GAIN_TRANSIENT_MAY_DUCK
         }
-      } ?: AudioManager.AUDIOFOCUS_GAIN_TRANSIENT
+      } ?: AudioManager.AUDIOFOCUS_GAIN
       audioFocusRequest = AudioFocusRequest.Builder(requestType).run {
         setAudioAttributes(
           AudioAttributes.Builder()
@@ -169,7 +175,7 @@
     } else {
       @Suppress("DEPRECATION")
       val requestType = if (interruptionMode == InterruptionMode.DO_NOT_MIX) {
-        AudioManager.AUDIOFOCUS_GAIN_TRANSIENT
+        AudioManager.AUDIOFOCUS_GAIN
       } else {
         AudioManager.AUDIOFOCUS_GAIN_TRANSIENT_MAY_DUCK
       }
@@ -372,6 +378,25 @@
         }
       }
 
+      /**
+       * The ExoPlayer's audio session, for attaching an `AudioEffect`.
+       *
+       * `android.media.audiofx.Equalizer` is constructed against a session id
+       * and there is no other way to reach this one — the player is created
+       * inside this module and never handed out. Session 0, the global output
+       * mix, is not an answer: it has been restricted since Android 9 and an
+       * effect attached to it is silently ignored on most devices.
+       *
+       * Read on the main thread, because ExoPlayer is built with the main
+       * looper and throws on access from any other — and read on demand rather
+       * than captured at construction, because the id is `AUDIO_SESSION_ID_UNSET`
+       * until the audio renderer is initialised. Ask before anything has
+       * played and the answer is a useless zero.
+       */
+      Property("audioSessionId") { player: AudioPlayer ->
+        runOnMain { player.ref.audioSessionId }
+      }
+
       Property("id") { player ->
         player.id
       }
@@ -483,6 +508,24 @@
       Function("pause") { player: AudioPlayer ->
         runOnMain {
           player.ref.pause()
+        }
+      }
+
+      /**
+       * Hand the player the track after this one, or nothing.
+       *
+       * The queue lives in JavaScript, and this is how it tells the player
+       * what is coming without giving up ownership: one item ahead, replaced
+       * whenever the queue changes, cleared when nothing follows.
+       */
+      Function("setNextSource") { player: AudioPlayer, source: AudioSource? ->
+        runOnMain {
+          val mediaSource = createMediaItem(source)
+          if (mediaSource == null) {
+            player.clearNextMediaSource()
+          } else {
+            player.setNextMediaSource(mediaSource)
+          }
         }
       }
 
diff --git a/node_modules/expo-audio/android/src/main/java/expo/modules/audio/AudioPlayer.kt b/node_modules/expo-audio/android/src/main/java/expo/modules/audio/AudioPlayer.kt
--- a/node_modules/expo-audio/android/src/main/java/expo/modules/audio/AudioPlayer.kt
+++ b/node_modules/expo-audio/android/src/main/java/expo/modules/audio/AudioPlayer.kt
@@ -10,6 +10,7 @@
 import androidx.media3.common.C
 import androidx.media3.common.PlaybackException
 import androidx.media3.common.PlaybackParameters
+import androidx.media3.common.MediaItem
 import androidx.media3.common.Player
 import androidx.media3.common.util.UnstableApi
 import androidx.media3.exoplayer.DefaultLoadControl
@@ -95,8 +96,57 @@
     ref.setMediaSource(source)
     ref.prepare()
     startUpdating()
+  }
+
+  /**
+   * Queue the item that follows this one, so ExoPlayer decodes it now and
+   * joins it to the current one when the current one ends.
+   *
+   * This is what closes the gap between tracks. `setMediaSource` tears the
+   * timeline down and rebuilds it, and the silence a listener hears is the
+   * new source being opened and its first buffer filled — work that, with a
+   * second item already in the timeline, ExoPlayer does ahead of time and
+   * joins sample-accurately for the formats that carry gapless metadata.
+   *
+   * The timeline is kept to exactly `[current, next]`: anything before the
+   * current item is a track that has finished, and anything after the next
+   * is a guess about a queue that JavaScript owns and may have changed.
+   */
+  fun setNextMediaSource(source: MediaSource) {
+    trimToCurrent()
+    ref.addMediaSource(source)
+  }
+
+  /** Nothing follows — the end of the queue, or repeat-one, or a reshuffle. */
+  fun clearNextMediaSource() {
+    trimToCurrent()
+  }
+
+  private fun trimToCurrent() {
+    val current = ref.currentMediaItemIndex
+    val count = ref.mediaItemCount
+    // After first, so the current index does not shift under the second call.
+    if (count > current + 1) ref.removeMediaItems(current + 1, count)
+    if (current > 0) ref.removeMediaItems(0, current)
   }
 
+  /**
+   * ExoPlayer moved to the next item by itself: the previous one ended.
+   *
+   * Reported as its own field rather than as `didJustFinish`, because the two
+   * mean different things to the app. `didJustFinish` is the player having
+   * stopped — there is nothing after this — and JavaScript answers it by
+   * loading whatever comes next. A transition is the player having *already*
+   * started what comes next, and the answer is bookkeeping, not a load.
+   */
+  override fun onMediaItemChanged(mediaItem: MediaItem?, reason: Int) {
+    if (reason == Player.MEDIA_ITEM_TRANSITION_REASON_AUTO) {
+      sendStatusUpdate(mapOf("trackTransition" to true))
+    } else {
+      super.onMediaItemChanged(mediaItem, reason)
+    }
+  }
+
   fun setActiveForLockScreen(active: Boolean, metadata: Metadata? = null, options: AudioLockScreenOptions? = null) {
     if (active) {
       this.metadata = metadata
diff --git a/node_modules/expo-audio/android/src/main/java/expo/modules/audio/AudioRecords.kt b/node_modules/expo-audio/android/src/main/java/expo/modules/audio/AudioRecords.kt
--- a/node_modules/expo-audio/android/src/main/java/expo/modules/audio/AudioRecords.kt
+++ b/node_modules/expo-audio/android/src/main/java/expo/modules/audio/AudioRecords.kt
@@ -110,7 +110,18 @@
 class AudioLockScreenOptions(
   @Field val showSeekForward: Boolean,
   @Field val showSeekBackward: Boolean,
-  @Field val isLiveStream: Boolean? = null
+  @Field val isLiveStream: Boolean? = null,
+  /**
+   * Previous and next in place of the seek buttons.
+   *
+   * The seek buttons are session custom commands, and a session that
+   * declares any custom button has its skip actions stripped from the legacy
+   * `PlaybackState` by media3 — so Bluetooth, the lock screen and Android's
+   * own media controls all lose next and previous. With this set the service
+   * declares no custom buttons at all, the skip actions stay advertised, and
+   * the notification on Android 12 and below carries previous and next.
+   */
+  @Field val showSkipControls: Boolean = false
 ) : Record
 
 enum class InterruptionMode(val value: String) : Enumerable {
diff --git a/node_modules/expo-audio/android/src/main/java/expo/modules/audio/service/AudioControlsService.kt b/node_modules/expo-audio/android/src/main/java/expo/modules/audio/service/AudioControlsService.kt
--- a/node_modules/expo-audio/android/src/main/java/expo/modules/audio/service/AudioControlsService.kt
+++ b/node_modules/expo-audio/android/src/main/java/expo/modules/audio/service/AudioControlsService.kt
@@ -90,6 +90,11 @@
 
         ACTION_SEEK_FORWARD -> currentPlayerRef.seekTo(currentPlayerRef.currentPosition + SEEK_INTERVAL_MS)
         ACTION_SEEK_BACKWARD -> currentPlayerRef.seekTo(currentPlayerRef.currentPosition - SEEK_INTERVAL_MS)
+        // The queue is the app's, so a skip is announced rather than
+        // performed — the same broadcast the session callback sends for a
+        // Bluetooth remote's buttons, so both arrive by one road.
+        ACTION_NEXT -> sendSkip("next")
+        ACTION_PREVIOUS -> sendSkip("previous")
       }
     }
 
@@ -161,13 +166,31 @@
     )
   }
 
+  /**
+   * A notification button, as a foreground-service start.
+   *
+   * `getService` is enough on stock Android, where a button on a notification
+   * earns the app a moment of background allowance. Some vendors do not grant
+   * it, and a plain `startService` from a backgrounded app is then refused
+   * outright — the button does nothing and nothing says why. A foreground
+   * start is always allowed to reach a service that promotes itself at once,
+   * which `onStartCommand` does before anything else.
+   */
   private fun buildActionPendingIntent(action: String): PendingIntent {
     val intent = Intent(this, AudioControlsService::class.java).setAction(action)
-    return PendingIntent.getService(
-      this,
-      action.hashCode(),
-      intent,
-      PendingIntent.FLAG_UPDATE_CURRENT or PendingIntent.FLAG_IMMUTABLE
+    val flags = PendingIntent.FLAG_UPDATE_CURRENT or PendingIntent.FLAG_IMMUTABLE
+    return if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {
+      PendingIntent.getForegroundService(this, action.hashCode(), intent, flags)
+    } else {
+      PendingIntent.getService(this, action.hashCode(), intent, flags)
+    }
+  }
+
+  private fun sendSkip(direction: String) {
+    sendBroadcast(
+      Intent(AudioMediaSessionCallback.ACTION_MEDIA_SKIP)
+        .setPackage(packageName)
+        .putExtra(AudioMediaSessionCallback.EXTRA_DIRECTION, direction)
     )
   }
 
@@ -192,6 +215,18 @@
       val compactViewIndices = mutableListOf<Int>()
       var currentIndex = 0
 
+      if (currentOptions?.showSkipControls == true) {
+        builder.addAction(
+          NotificationCompat.Action(
+            androidx.media3.session.R.drawable.media3_icon_previous,
+            "Previous",
+            buildActionPendingIntent(ACTION_PREVIOUS)
+          )
+        )
+        compactViewIndices.add(currentIndex)
+        currentIndex++
+      }
+
       if (currentOptions?.showSeekBackward == true) {
         builder.addAction(
           NotificationCompat.Action(
@@ -227,9 +262,22 @@
           )
         )
         compactViewIndices.add(currentIndex)
+        currentIndex++
       }
 
-      style.setShowActionsInCompactView(*compactViewIndices.toIntArray())
+      if (currentOptions?.showSkipControls == true) {
+        builder.addAction(
+          NotificationCompat.Action(
+            androidx.media3.session.R.drawable.media3_icon_next,
+            "Next",
+            buildActionPendingIntent(ACTION_NEXT)
+          )
+        )
+        compactViewIndices.add(currentIndex)
+      }
+
+      // The compact view holds three buttons at most.
+      style.setShowActionsInCompactView(*compactViewIndices.take(3).toIntArray())
     }
 
     builder.setStyle(style)
@@ -240,6 +288,22 @@
     val session = mediaSession ?: return
     val mediaButtons = mutableListOf<CommandButton>()
 
+    /*
+     * No custom buttons at all when the app wants previous and next.
+     *
+     * media3 strips `ACTION_SKIP_TO_NEXT` and `ACTION_SKIP_TO_PREVIOUS` from
+     * the legacy playback state the moment the session declares any media
+     * button preference — the seek buttons here included — unless a slot
+     * reservation says otherwise. Declaring nothing leaves the skip actions
+     * exactly as the player's commands advertise them, and Android 13+ draws
+     * previous, play/pause and next from those on its own.
+     */
+    if (currentOptions?.showSkipControls == true) {
+      session.setCustomLayout(mediaButtons)
+      session.setMediaButtonPreferences(mediaButtons)
+      return
+    }
+
     // Add seek backward button if enabled
     if (currentOptions?.showSeekBackward == true) {
       mediaButtons.add(
@@ -371,7 +435,7 @@
           updateMetadata(metadata)
         }
         val session = MediaSession.Builder(context, sessionPlayer)
-          .setCallback(AudioMediaSessionCallback())
+          .setCallback(AudioMediaSessionCallback(context))
           .build()
 
         // Replace the basic media session with a session connected to our playback service.
@@ -452,7 +516,7 @@
           updateMetadata(metadata)
         }
         val session = MediaSession.Builder(context, sessionPlayer)
-          .setCallback(AudioMediaSessionCallback())
+          .setCallback(AudioMediaSessionCallback(context))
           .build()
 
         player.mediaSession.release()
@@ -510,6 +574,24 @@
     notificationManager.cancel(notificationId)
   }
 
+  /**
+   * The app was swiped out of the recents list.
+   *
+   * A media service is allowed to outlive its task, and this one does: the
+   * notification carries on playing with no activity behind it, the process
+   * stays up, and the next launch comes back to a JavaScript runtime that has
+   * lost its host — a blank screen that only "force stop" clears.
+   *
+   * Backgrounding is not this. An app behind another app, or with the screen
+   * off, has its task intact and never gets here; only removing the task does.
+   * So the app is told, and it decides what to do about it — there are
+   * statistics to write before anything shuts down.
+   */
+  override fun onTaskRemoved(rootIntent: Intent?) {
+    sendBroadcast(Intent(ACTION_TASK_REMOVED).setPackage(packageName))
+    super.onTaskRemoved(rootIntent)
+  }
+
   override fun onDestroy() {
     super.onDestroy()
 
@@ -551,6 +633,8 @@
   }
 
   companion object {
+    /** Must match `AppLifecycleModule` in the app. See `onTaskRemoved`. */
+    const val ACTION_TASK_REMOVED = "expo.modules.audio.TASK_REMOVED"
     private const val CHANNEL_ID = "expo_audio_channel"
     private const val ACTION_PLAY = "expo.modules.audio.action.PLAY"
     private const val ACTION_PAUSE = "expo.modules.audio.action.PAUSE"
@@ -558,6 +642,8 @@
 
     const val ACTION_SEEK_FORWARD = "expo.modules.audio.action.SEEK_FORWARD"
     const val ACTION_SEEK_BACKWARD = "expo.modules.audio.action.SEEK_BACKWARD"
+    private const val ACTION_NEXT = "expo.modules.audio.action.NEXT"
+    private const val ACTION_PREVIOUS = "expo.modules.audio.action.PREVIOUS"
 
     const val SEEK_INTERVAL_MS = 10000L
   }
diff --git a/node_modules/expo-audio/android/src/main/java/expo/modules/audio/service/AudioMediaSessionCallback.kt b/node_modules/expo-audio/android/src/main/java/expo/modules/audio/service/AudioMediaSessionCallback.kt
--- a/node_modules/expo-audio/android/src/main/java/expo/modules/audio/service/AudioMediaSessionCallback.kt
+++ b/node_modules/expo-audio/android/src/main/java/expo/modules/audio/service/AudioMediaSessionCallback.kt
@@ -1,5 +1,7 @@
 package expo.modules.audio.service
 
+import android.content.Context
+import android.content.Intent
 import android.os.Bundle
 import androidx.media3.common.Player
 import androidx.media3.session.MediaSession
@@ -10,7 +12,7 @@
 import com.google.common.util.concurrent.ListenableFuture
 
 @OptIn(UnstableApi::class)
-class AudioMediaSessionCallback : MediaSession.Callback {
+class AudioMediaSessionCallback(private val context: Context) : MediaSession.Callback {
   override fun onConnect(
     session: MediaSession,
     controller: MediaSession.ControllerInfo
@@ -24,11 +26,12 @@
             .add(Player.COMMAND_SEEK_IN_CURRENT_MEDIA_ITEM)
             .add(Player.COMMAND_SEEK_FORWARD)
             .add(Player.COMMAND_SEEK_BACK)
-            // Remove track navigation commands
-            .remove(Player.COMMAND_SEEK_TO_PREVIOUS_MEDIA_ITEM)
-            .remove(Player.COMMAND_SEEK_TO_NEXT_MEDIA_ITEM)
-            .remove(Player.COMMAND_SEEK_TO_PREVIOUS)
-            .remove(Player.COMMAND_SEEK_TO_NEXT)
+            // Track navigation, answered by the app rather than by the player:
+            // the queue lives in JavaScript and this player holds one item.
+            .add(Player.COMMAND_SEEK_TO_PREVIOUS_MEDIA_ITEM)
+            .add(Player.COMMAND_SEEK_TO_NEXT_MEDIA_ITEM)
+            .add(Player.COMMAND_SEEK_TO_PREVIOUS)
+            .add(Player.COMMAND_SEEK_TO_NEXT)
             .build()
         )
         .setAvailableSessionCommands(
@@ -43,6 +46,37 @@
     }
   }
 
+  /**
+   * Turn a skip into a broadcast instead of a seek.
+   *
+   * The session's player holds exactly one media item, so seeking to the next
+   * one means nothing to it — the queue is in JavaScript. The request is
+   * announced here and refused, and the app's own module hands it over.
+   */
+  override fun onPlayerCommandRequest(
+    session: MediaSession,
+    controller: MediaSession.ControllerInfo,
+    playerCommand: Int
+  ): Int {
+    val direction = when (playerCommand) {
+      Player.COMMAND_SEEK_TO_NEXT, Player.COMMAND_SEEK_TO_NEXT_MEDIA_ITEM -> "next"
+      Player.COMMAND_SEEK_TO_PREVIOUS, Player.COMMAND_SEEK_TO_PREVIOUS_MEDIA_ITEM -> "previous"
+      else -> null
+    } ?: return super.onPlayerCommandRequest(session, controller, playerCommand)
+
+    context.sendBroadcast(
+      Intent(ACTION_MEDIA_SKIP)
+        .setPackage(context.packageName)
+        .putExtra(EXTRA_DIRECTION, direction)
+    )
+    return SessionResult.RESULT_ERROR_NOT_SUPPORTED
+  }
+
+  companion object {
+    const val ACTION_MEDIA_SKIP = "expo.modules.audio.MEDIA_SKIP"
+    const val EXTRA_DIRECTION = "direction"
+  }
+
   override fun onCustomCommand(
     session: MediaSession,
     controller: MediaSession.ControllerInfo,
diff --git a/node_modules/expo-audio/android/src/main/java/expo/modules/audio/service/MetadataInjectingPlayer.kt b/node_modules/expo-audio/android/src/main/java/expo/modules/audio/service/MetadataInjectingPlayer.kt
--- a/node_modules/expo-audio/android/src/main/java/expo/modules/audio/service/MetadataInjectingPlayer.kt
+++ b/node_modules/expo-audio/android/src/main/java/expo/modules/audio/service/MetadataInjectingPlayer.kt
@@ -36,6 +36,45 @@
     super.removeListener(forwardingListener ?: listener)
   }
 
+  /**
+   * Advertise track navigation even though this player holds one item.
+   *
+   * `MediaSession` intersects what the callback allows with what the player
+   * reports, and ExoPlayer reports no next item because there is none in its
+   * timeline — the queue is in JavaScript. Without this the session's legacy
+   * `PlaybackState` carries neither `ACTION_SKIP_TO_NEXT` nor
+   * `ACTION_SKIP_TO_PREVIOUS`, and a Bluetooth remote's skip buttons are
+   * dropped by the framework before anything here is consulted. The commands
+   * are never executed against this player: the callback answers and refuses
+   * them.
+   */
+  override fun getAvailableCommands(): Player.Commands =
+    super.getAvailableCommands()
+      .buildUpon()
+      .add(Player.COMMAND_SEEK_TO_NEXT)
+      .add(Player.COMMAND_SEEK_TO_NEXT_MEDIA_ITEM)
+      .add(Player.COMMAND_SEEK_TO_PREVIOUS)
+      .add(Player.COMMAND_SEEK_TO_PREVIOUS_MEDIA_ITEM)
+      .build()
+
+  /** `ForwardingPlayer` answers this from the wrapped player, not from above. */
+  override fun isCommandAvailable(command: Int): Boolean = when (command) {
+    Player.COMMAND_SEEK_TO_NEXT,
+    Player.COMMAND_SEEK_TO_NEXT_MEDIA_ITEM,
+    Player.COMMAND_SEEK_TO_PREVIOUS,
+    Player.COMMAND_SEEK_TO_PREVIOUS_MEDIA_ITEM -> true
+    else -> super.isCommandAvailable(command)
+  }
+
+  /*
+   * The legacy `PlaybackState` that Bluetooth reads sets its skip actions from
+   * the timeline, not only from the commands. One media item means no next and
+   * no previous, so both bits stayed clear whatever the commands said.
+   */
+  override fun hasNextMediaItem(): Boolean = true
+
+  override fun hasPreviousMediaItem(): Boolean = true
+
   override fun getMediaMetadata(): MediaMetadata {
     val metadata = injectedMetadata
     return super.getMediaMetadata()
